[build]
builder = "RAILPACK"

[deploy]
startCommand = "uvicorn equity_strategist.api.server:app --app-dir src --host 0.0.0.0 --port $PORT --workers 1"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
